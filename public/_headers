# Cloudflare Pages — per-path response headers.

# /our-work is a private portfolio; in addition to the noindex meta tag
# on the page itself, send it as an HTTP header so crawlers respect it.
/our-work
  X-Robots-Tag: noindex, nofollow

/our-work/*
  X-Robots-Tag: noindex, nofollow
