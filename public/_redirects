# Cloudflare Pages _redirects — applied at the edge before the static asset lookup.
# Format: <source-path> <destination> <status>

# Projects page was removed pre-launch. Any inbound link to /projects (search
# engines, prior bookmarks, the old LeadConnector nav) goes home with a 301.
/projects  /  301
/projects/  /  301
