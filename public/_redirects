# Cloudflare Pages _redirects — applied at the edge before the static asset lookup.
# Format: <source-path> <destination> <status>
#
# Currently empty. The Projects page exists at /projects (unlisted from the primary
# nav but reachable by direct URL); no path-level redirects are needed at launch.
# Add entries here only when a real legacy URL pattern surfaces.
